# Changelog

All notable changes to this repository are documented here.

This repository uses Semantic Versioning. Entries are one flat list per version — one entry per user-visible change, one sentence, ending with the issue or PR reference where the full story lives.

## [Unreleased]

- Fixed the empty-lock install-publication test deterministic by pinning the lock file's mtime before the call, so its outcome no longer depends on `Date.now()` and the filesystem mtime agreeing within a few milliseconds (#565)
- Fixed the suite supervisor exiting with only its start line when an internal error occurred after the run loop — it now prints its truthful summary on stdout with the cause on stderr, and a cleanup failure no longer aborts the remaining bounded removals (#566)
- Added a `stream` stdin mode to the shared bounded executor's interactive-child support, alongside the existing write-then-close delivery (#542)
- Added truthful macOS baseline observation to qualification records: one read-only `sw_vers -productVersion` probe at admission gates completion, and a baseline without an observed macOS identity never reports success (#540)
- Changed declared Node support from the open-ended `>=22` to `>=22 <23`, pinned `node-version: 22` in CI and the release workflow, and stated the primary macOS/Node 22 baseline in the README (#540)
- Added one compact structured qualification record per supervised run, carrying source identity, truthful runtimes, selection evidence, and retained CI diagnostics (#539)
- Added provenance validation for supplied package archives: missing, malformed, digest-mismatched, or stale source records are rejected before any test runs (#538)
- Added one canonical source/artifact identity contract (`test/support/package-identity.ts`) with a from-source creator (`scripts/create-package-candidate.ts`) whose single bounded build replaces the release workflow's separate typecheck, build, and pack steps (#538)
- Changed package preparation to be lazy and supervisor-owned: the first consumer that executes triggers exactly one bounded build plus `npm pack` per invocation, so pure focused or filtered selections never prepare a package (#537)
- Added preparation evidence (status, requests, durations, cleanup, failure diagnostics) to qualification results, run logs, and summaries (#537)
- Changed full and stress suite selections to derive from the current test corpus and run as explicit paths, with junit execution evidence gating a green exit on executed coverage matching the selection (#536)
- Added pre-run enforcement of the pinned Bun toolchain version (package.json `engines.bun`), installed by CI via `bun-version-file: package.json` (#536)
- Added rejection of repository `bunfig.toml` `[test]` sections and unparseable bunfig files before any run (#536)
- Added validated budget overrides for the supervised suite: `APKIT_TEST_PER_RUN_DEADLINE_MS` in every mode, plus `APKIT_TEST_AGGREGATE_DEADLINE_MS` and `APKIT_TEST_MAX_RUNS` in stress mode, with actionable errors for invalid values (#535)
- Changed the default per-run deadline from 300s to 600s and made the stress aggregate `max-runs × per-run`, so every default repetition fits; run logs now record the effective policy and a distinct cleanup duration (#535)
- Changed scanning views (concise `status`, default receipts, `list projects`, confirmation reviews) to name each Project by the shortest distinguishing path segments, keeping full stable paths in `--verbose`, `--json`, and executable command arguments (#504)
- Improved `list projects` to render aligned Project/Profile/Hosts/State rows with a counted summary footer and one `problem` line per entry (#504)
- Added a generated-source notice to every installed Skill document and composed Context output, and made the claimed OpenCode configuration file `.opencode/opencode.jsonc` open with format-valid JSONC comment guidance (#503)
- Changed the default `update` receipt to state its impact once (`Updated 2 Projects (3 generated files).`), routing complete per-run evidence through `Details: apkit details` (#502)
- Added automatic lifecycle operation history at `~/.agents/agent-profile-kit/operation-history.json`, retaining the latest 200 attempted `install`, `update`, and `uninstall` runs with per-Project outcomes and never file contents (#501)
- Added `apkit details [--list | <operation-id>] [--json]` to read that history read-only (#501)
- Added `apkit configure profile` for interactive or explicit (`--context`/`--skill`) Profile membership changes that never install (#500)
- Added interactive uninstall: bare `apkit uninstall` opens the searchable Project picker instead of refusing, then offers whole-installation or selected-Host removal with a full scope review before the general confirmation (#499)
- Added `--host` narrowing to `apkit uninstall` for removing individual Hosts while preserving shared output required by remaining Hosts; removing the last Host uses the complete-uninstall result (#498)
- Changed `apkit uninstall` to remove selected Projects and forget their recorded selection in one per-Project transition, with explicit scope flags, an interactive confirmation, and shared consent for deleting changed generated output; public `unbind` is retired — every `unbind` form exits 1 naming `apkit uninstall` (breaking) (#496)
- Added bare interactive `install` that collects only missing Profile/Host selections through searchable pickers (#495)
- Changed `apkit install` to target the current Project by default and install with its remembered selection after one general confirmation, with the shared consent contract for changed files; public `bind` is retired — every `bind` form exits 1 naming `apkit install` (breaking) (#494)
- Changed `update` to require explicit per-operation changed-file consent: `--replace-changed` for replacement and `--remove-changed` for deletion, ending silent non-interactive replacement; `--auto-confirm` authorizes neither (#493)
- Changed the public `apply` command to `update`, keeping scope, filters, `--json`, and `--replace-changed` behavior; every `apply` form exits 1 naming `apkit update` (breaking) (#492)
- Fixed printed lifecycle next-action commands to render the Project's home-relative identity as one shell-quoted POSIX token, so the printed action — including paths containing spaces — executes exactly as printed instead of exiting with an invalid-path error (#489, #461)
- Added a one-sentence Project-local verification step after a successful apply: start a new session of each configured Host and ask what Profile material it loaded (#457)
- Added an authoring handoff after a first run that installed the scaffolded example Profile, pointing at `apkit new skill`, `apkit new context`, and `apkit new profile` (#456)
- Added optional guided first-Profile selection during `init` — a yes/no offer, a Profile name, and multi-select choices of existing Context Modules and Skills — printing the equivalent `apkit new profile` command on completion (#460)
- Added interactive prompts for missing `bind` arguments: Profile and Host pickers with advisory installed/absent detection, printing the equivalent fully specified command on completion (#459)
- Added `apkit new profile <name> [--context <id>]... [--skill <id>]...` to scaffold a bindable Profile at `profiles/<name>.yaml`, refusing unknown selections and zero selections before any write (#454)
- Added `apkit new context <name>` to scaffold a Context Module at `context/<name>.md`, preflighted through the canonical schema with occupied or symlinked destinations refused (#453)
- Changed bare `apkit` invocation to a read-only entry screen summarizing current setup state and task-relevant commands instead of the full manual (#452)
- Added changed-output confirmation to interactive `apply`: it names every affected changed generated file and asks before any write, printing the equivalent `--replace-changed` command after confirmation (#458)
- Added `--stale` and `--blocked` selection filters for `status` and `apply`, governing reports, machine JSON, and write scope identically (#455)
- Removed the `--blockers-only` flag in favor of `--blocked` (breaking) (#455)
- Changed Apply Receipts to name every committed file operation with its Project attribution, without the former concise path or affected-Project caps (#451)
- Improved invalid-Workspace-reference errors to carry the offending authoring file, the invalid value, and available names with a nearest-name suggestion (#450)
- Fixed `PAGER` normalization at the pager boundary to follow the POSIX double-quote contract exactly, treat an explicitly empty `LESS` as operator authority, and handle repeated SIGINT/SIGTERM with 130/143 exit mapping (#476)
- Fixed the receipt-proven Workspace source-change cause disappearing from verbose diagnostics when generated output stays byte-identical and drift masks the source state (#477, #449)
- Changed verbose lifecycle output to focused diagnostics that omit composed Context bodies, unchanged output paths, and per-Project setup provenance while preserving per-output drift causes and actionable setup steps (#449)
- Added paging of long interactive `apkit guide` output through the configured `PAGER`, with redirected output staying byte-identical and pager failures printing the unchanged guidance plus one advisory (#448)
- Changed authoring guides to identify the configured Workspace location before example creation instructions across `profile`, `context`, and `skill` topics (#447)
- Added `apkit open` to open the configured Workspace in the system file manager with structured recovery on opener failures (#446)
- Added `apkit new skill <name>` to scaffold a Skill and print the created `SKILL.md` path, refusing occupied or symlinked destinations with typed diagnostics (#445)
- Fixed space-containing Workspace paths in focused authoring guides rendering as atomic path tokens so they do not split across lines (#475)
- Fixed `workspace-open-failed` errors to preserve `cleanupFailed` evidence and provide a runnable `cd <workspace>` recovery command (#474)
- Fixed `apkit new skill` failure recovery: the occupied-destination diagnostic names a runnable recovery command, pre-open failures never remove user content, and a failed post-write close reports failed creation instead of a success receipt (#473)
- Fixed Host CLI probes that could hang `init` when an executable ignored SIGTERM by bounding every Adapter probe through the shared process executor with a 1 MiB per-stream output budget (#472, #444)
- Improved `init` guidance to match the machine: home-relative Workspace presentation, a one-sentence Profile explanation, advisory Host detection across all six supported Hosts, and a suggested bind command for the first detected Host (#444)
- Added the `-v` short flag reporting the packaged engine version identically to `--version` (#443)
- Changed root help to list human commands without flag inventories, omitting machine-facing commands from the default listing while keeping them accessible via `apkit machine` (#442)
- Improved CLI error documents with consistent recovery guidance (what happened, why, what to type, usage) and newcomer vocabulary that no longer leaks internal configuration paths (#441)
- Changed every Blocker's presentation to one newcomer-worded sentence with an evidence-derived runnable recovery command, retiring the concise verbose-pointer redirect (#440)
- Changed warnings to render inline beside command outcomes across status, apply, teardown, and temporary views instead of separate titled sections, and suppressed warning text under `--blockers-only` (#439, #467)
- Changed `status` and `apply` to default to fleet scope with `--here` for explicit current-directory scope, rejecting conflicting scope arguments before execution (#436)
- Changed concise lifecycle status to a complete primary-cause fleet partition across the five canonical causes with all actionable Project identities and settled counts (#435)
- Fixed duplicate Project inventory problem formatting by delegating to the canonical error wording boundary (#466)
- Fixed binding receipts rendering recognizable Project identities when invoked inside the bound Project instead of error-shaped dot aliases (#437)
- Fixed lifecycle recovery retry guidance preserving the invoked scope, so blocked scoped runs no longer recommend widening to fleet retry (#464)
- Fixed concise needs-attention output to nest each member under its primary-cause frame with attributed Blocker remedies when Projects share an affected path, keeping removal reasons for later teardown Projects in mixed fleets (#435, #462)
- Fixed missing-vs-changed drift classification when unchanged siblings exist, and duplicate Project member identities across concise blocker and next-action output (#435, #462)
- Improved Project inventory rendering as aligned rows with short unambiguous identity, Profile, Hosts, and state, plus a summary footer (#438)
- Fixed CLI diagnostics that shattered into one line per protected value by rendering every CLI-boundary document — help, guides, receipts, errors — through the presentation renderer, restoring readable sentences with unchanged machine JSON and exit codes (#390)
- Changed location display to an explicit fleet or project scope so fleet listings and next actions use stable home-relative identities instead of cwd aliases (#385)
- Changed Blockers to carry typed facts only, with every problem, requirement, and remedy sentence composed at the presentation boundary in newcomer vocabulary and a runnable command appended where the remedy names none (#382)
- Fixed Project target validation to accept a valid final symlink to a directory again, and surfaced Installation State read failures as typed facts on every report and error surface (#382)
- Changed Ownership State reads to accept the previous schema version, ignoring legacy ownership-token entries; back up `~/.agents/agent-profile-kit/state/manifest.json` before running this version and restore it before downgrading — the prior v6-only release cannot read the republished schema 7 (#374)
- Removed the Installation Marker so the Installation Receipt is the sole ownership home; changed extant roots without a matching recorded hash now fail closed instead of being refreshed or removed (breaking) (#374)
- Changed identity-proven generated output roots to be disposable: drift reports as non-blocking pending work and `apply` replaces the whole proven root from Workspace source (ADR-0019, ADR-0022) (#363)
- Added Pi support for temporary Profile installations via `install-temp --host pi` through the canonical registry and receipt-owned lifecycle (#369)
- Added cross-Project grouping of semantic warnings in concise and verbose lifecycle views, reporting affected-Project counts concisely and all paths in verbose output (#354)
- Fixed warning groups to sort deterministically by kind, consequence, and copyable values (#354)
- Added grouped tracked-path Blocker evidence with the exact user-owned `git -C <project> rm -r --cached --` untracking command in focused verbose views (#353)
- Fixed the untracking command to bind to the Blocker's Project root with `git -C` and include `-r` so directory roots untrack without a recursion error (#353)
- Added the focused `--blockers-only` view to `apply`, rendering the committed receipt and still-pending Projects before focused Blocker evidence (#352)
- Added a focused human `status --blockers-only` view showing only global and Project Blockers in the selected scope (#351)
- Added `apkit bind --replace` to update an existing Project Binding's Profile and Host set with an old → new receipt (#342)
- Added OpenCode as a supported Agent Host installing allowed-invocation Skills through the qualified shared projection (#330)
- Added composed Profile Context loading for OpenCode through a dedicated whole-file JSONC configuration (#331)
- Added explicit-only OpenCode Skill invocation enforced through Artifact-ID-keyed global deny rules (#332)
- Added duplicate Skill discovery between Claude and OpenCode as a non-blocking diagnostic warning (#333)
- Improved OpenCode qualification coverage across temporary installation, uninstallation, and exclusion contributions (#334)
- Added weekly grouped Dependabot updates for the npm/Bun dependency graph and SHA-pinned GitHub Actions (#283)
- Added retained supervised suite diagnostics in CI, uploaded for 7 days only when the test step fails or is cancelled, with attempt-unique artifact names (#281)
- Added Safe Repair handling for a missing exclusion section, file, or safe Git parent during intentional-deletion retirement, retiring the `repository-exclusion-section-missing` Blocker kind (lifecycle JSON schema 12) (#350)
- Added Safe Repair handling for exclusion contributions whose Git target moved, removing old-target entries and republishing at the new target when both are proven (lifecycle JSON schema 11) (#349)
- Added Safe Repair handling for stale exclusion entries at an unchanged proven Git target, replaced atomically per installation (lifecycle JSON schema 10) (#348)
- Added Safe Repair handling for a provably missing Repository Exclusion Contribution on an installation now inside a Git Project, with one typed eligibility boundary for all repair classes (lifecycle JSON schema 9) (#347)
- Changed `repository-exclusion-contribution` to the only structured Blocker kind for receipt-owned Git exclusion evidence, advancing the lifecycle and temporary-installation payload families to schema 8 (#346)
- Changed the composed Profile Context envelope to compact Profile metadata plus normalized Context Module bodies, shared by Pi, Codex, Claude, and Grok; upgrading changes envelope hashes, so `status` reports Context drift until `apply` (#326)
- Changed disabled-invocation Skill projection to preserve authored YAML frontmatter comments and formatting, unifying the Claude and shared emitters (#329)
- Qualified the complete quiet, task-first newcomer journey through one packed CLI path and recaptured the living first-run excerpts from that run (#307)
- Changed default views, help, inventory, and lifecycle summaries to apply the maintained newcomer presentation lexicon (#306)
- Added an invocation-wide next-launch readiness statement on successful changed apply, naming one Profile when unambiguous (#305)
- Changed first-use Host setup guidance to one grouped note in concise apply, suppressing standing trust and root-launch guidance during routine updates (#304)
- Changed successful apply to lead from its committed-receipt impact, summarizing affected Project and generated-file counts once and reserving "already current" for true no-op applies (#303)
- Changed concise status to omit transition-triggered and standing Host Setup Steps, with `--verbose` and JSON retaining every step (#302)
- Changed unblocked concise status to one compact scope-correct decision, moving routine generated paths and Git bookkeeping behind a matching `--verbose` route (#301)
- Added printing the exact `apkit remove-temp <actual-id>` command after a successful temporary install (#300)
- Changed successful validation guidance to point at `apkit bind` when no Projects are configured and `apkit status` otherwise (#299)
- Changed focused initialization help and successful scaffold initialization to align on `apkit bind example --host codex` (#298)
- Changed redundant inventory `Next:` actions to instructional usage guidance (#297)
- Changed Host inventory to lead with the Hosts supported for configured Projects (#296)
- Changed the no-topic inventory menu to show each topic and its description once (#295)
- Changed bare and root help to lead with the four-step first run, separating common commands from secondary inventory, teardown, and machine commands (#294)
- Changed CI to install the frozen dependency graph with lifecycle scripts disabled (#282)
- Changed CI to typecheck, bundle, and create the package once each, reusing the actual archive across the supervised package and CLI boundary tests (#280)
- Changed CI to skip the complete macOS gate for draft pull requests and run it when a draft becomes ready, without letting contributor-controlled checkouts retain repository credentials (#279)
- Changed `uninstall` to proceed per Project: a Project whose owned output cannot be fully removed is kept with its reason while every other Project still removes; only a staging rollback failure aborts the invocation (#381)
- Removed the separate Safe Repair classification: a wholly absent owned generated file is ordinary non-blocking pending work that `apply` restores like any other write (breaking; lifecycle JSON schema 14) (#380)
- Changed Repository Exclusion Contribution to derived, best-effort bookkeeping that can never block: `apply` rewrites the owned section of `.git/info/exclude` from active receipts on every run, and unprovable targets produce one warning at exit 0 (breaking; Installation State schema 9 — back up state before upgrading, restore before downgrading) (#379)
- Changed Temporary Profile Installation commands to a machine-facing namespace — `apkit machine install-temp`, `apkit machine remove-temp`, `apkit machine list temporary` — with top-level forms failing fast with a pointer (breaking) (#378)
- Changed `init` to scaffold exactly the delivered artifact directories (`profiles`, `context`, `skills`), leaving pre-existing undelivered directories untouched (#377)
- Changed Agent Host capability probing to advisory-only: results surface as `host-attention` warnings and never gate planning or writing, and `status` performs no Host process execution (breaking; lifecycle JSON schema 13) (#376)
- Changed `unbind` to retire the Project's active Installation Receipt in the same operation, so no active receipt can outlive its binding; `apply` also re-ingests Local Configuration under the lifecycle lock so a concurrent `bind`/`unbind` can never resurrect a retired receipt (Installation State schema 8 — back up `~/.agents/agent-profile-kit/state/manifest.json` before upgrading, restore before downgrading) (#375)
- Fixed unprovable Git topology to produce one warning at exit 0 with installation proceeding instead of blocking; corrupt Git boundaries still fail closed (#404)
- Fixed duplicate Host capability warnings to one per Host per invocation, naming the Host and the strictest version it requires (#403)
- Fixed Ownership State reads to accept schema versions 6 and 7, so upgrading directly from v0.132.0 keeps existing receipts working without re-binding or re-applying (#402)
- Fixed residual `installation-ownership` Blocker evidence to state only what the evidence proves (#363, #364)
- Removed the non-actionable Claude/OpenCode duplicate-Skill discovery warning at the Adapter source (#363)
- Fixed standing first-use trust and root-launch guidance not replaying when an established Project/Host pairing later gains additional Host-consumed output (#324)
- Changed runtime ownership reads to accept only strict schema-6 JSON, closing the pre-1.0 Installation State migration window with focused 0.95.0 migration guidance for leftover YAML (#257)
- Removed the leftover special-case error path for the removed `apkit preview` command, so `preview` invocations fall through to ordinary unknown-command handling (#418)

## [0.95.0] - 2026-08-24

- Changed all ordinary and Temporary Profile Installation ownership to strict schema-6 JSON receipts with exact-byte self-validation and atomic replacement (breaking) (#254)
- Added the final minimal ownership-receipt model with a strict deterministic bounded JSON codec and pure supported-YAML normalization, without changing runtime publication (#253)
- Changed `status` to the complete apply-equivalent read-only plan and removed the separate `preview` command with its JSON variant (breaking; lifecycle JSON schema 7) (#252)
- Changed `apply --all` to commit and verify healthy Projects sequentially while leaving Project-scoped capability, ownership, and Git blockers untouched; global blockers still stop every write and partial blockers exit 2 (breaking; lifecycle JSON schema 6) (#251)
- Changed `status` and `apply` to target the bound Project containing the current directory by default, accepting one explicit bound root and requiring `--all` for fleet scope (breaking) (#250)
- Changed lifecycle text to render directly from nested global and Project records with structured blocker evidence and typed warning values (#249)
- Added Antigravity Skill delivery through the shared `.agents/skills/<Artifact ID>` projector with distinct Skills-only and combined Capability Contracts, and made Antigravity Skill rejection mandatory at the desired-state boundary (#231)
- Added Antigravity as the `antigravity` Agent Host through `agy` 1.1.13+, delivering always-on Context rules under `.agents/rules` (#230)
- Added a qualified shared `.agents/skills/<Artifact ID>` projector for Codex that preserves package content and composes disabled-invocation policy for both Host fields (#228)
- Added fleet-wide qualification instrumentation (`installer/qualification-instrumentation.ts`) and warm benchmark samples (`installer/benchmark.ts`) recorded as release evidence (#205)
- Changed successful `bind` and `unbind` receipts to compact task language with the short Project identity, visible Profile and Hosts, and a `preview` pointer (#204)
- Changed lifecycle summaries, next actions, and readiness to finish preview and apply with one compact actionable frame (#203)
- Added typed `transition`/`standing` provenance to Host Setup Steps, presenting transition-triggered steps only when the plan changes their output (#202)
- Changed shared fleet changes to render once with progressive Project scope instead of repeating each shared Workspace change per Project (#201)
- Added bounded concurrent inspection of independent Projects (fixed limit of four) through one invocation-scoped scheduler, with writes and state publication still sequential (#200)
- Added typed lifecycle impacts derived from prior receipt provenance and published in versioned lifecycle JSON (#199)
- Added one normalized ownership inspection per owned output per reconciliation pass, cached invocation-scoped with complete expected-output identity keys (#198)
- Added one capability probe per unique machine-level Host requirement per invocation instead of per Project (#197)
- Added batched Git and exclusion inspection per lifecycle pass, streaming each worktree index once with binary-search classification and no `maxBuffer` ceilings (#196)
- Added invocation-scoped reuse of Profile resolution, artifact fingerprints, Skill packages, composed Context, and Host projections (#195)
- Added normalized artifact fingerprints and typed output origins to Installation Manifest receipts (manifest schema v3 — back up `~/.agents/agent-profile-kit/state/manifest.yaml` before the first 0.68.0+ write, restore it before downgrading) (#194)
- Added ADR-0016 recording the responsive-terminal-presentation, read-only-inventory, and typed-blocker boundaries, with a packed discovery-to-lifecycle acceptance journey (#173)
- Changed reconciliation to publish global Blockers plus one complete record per Project (lifecycle JSON schema 5) (#248)
- Changed Temporary Profile Installation checks, warnings, contracts, setup steps, and outputs to route through each Host's canonical registered Adapter (#247)
- Changed Grok capability probing, inspection, warnings, topology, setup, and planning to route through its complete Adapter (#246)
- Changed Pi and Antigravity lifecycle planning to route through their registered complete Adapters, leaving Installer legacy dispatch only for Grok (#245)
- Added the canonical Host registry and complete ordinary-planning Adapter contract, migrating Claude and Codex behind it (#244)
- Changed multi-Host output coalescing to match solely on normalized physical identity — path, type, mode, and exact bytes — retaining all consuming Hosts (#243)
- Changed `uninstall` to be independent of Workspace and binding input, removing all ownership-proven output and exclusion contributions (breaking) (#242)
- Changed generated-directory ownership to one deterministic aggregate-root hash with drift reported only at the root (breaking; lifecycle JSON schema 4) (#241)
- Changed lifecycle summaries to derive directly from observable additions, updates, repairs, removals, blockers, and affected Projects, removing the `impacts` collection (breaking; lifecycle JSON schema 3) (#240)
- Removed the unsupported `agents`, `hooks`, and `tools` Profile fields; Profiles now contain exactly `id`, `context`, and `skills` (breaking) (#239)
- Changed Pi Skill installation from `.pi/skills/<Artifact ID>/` to the shared `.agents/skills/<Artifact ID>/` projection (breaking — retain a state backup before the first apply, then use the documented 0.81.0 rollback procedure if needed) (#229)
- Changed the Blocker contract to exhaustively structured typed records — kind, scope, problem, requirement, remedy, affected items — so message-only blockers can no longer be emitted (lifecycle and temporary-installation JSON `schemaVersion: 2`) (#172)
- Fixed hostile or oversized YAML Installation State to fail closed, bounding supported recovery to 8 MiB files and 100,000 aliases with alias-free exact-byte publication (#238)
- Fixed descendant cleanup verification to use the executor's canonical group-empty result instead of treating a terminated orphan awaiting reaping as a live leak (#211)
- Fixed Project path presentation to use one shortest-unambiguous presenter across every human command, with blocked temporary-installation diagnostics falling back to home-relative identity when the cwd is inside the Project (#171)
- Added a bounded, diagnostic process-execution boundary for every packed-CLI and PTY test child: process-group leadership, finite deadline, SIGKILL escalation, and typed results with captured evidence (#191)
- Changed `bun run test`, `bun run test:focused`, and `bun run test:stress` to the canonical supervised command surface with concise summaries and retained diagnostic logs, replacing handwritten repetition loops (#192)
- Added delayed ephemeral progress for interactive long-running `status` and `preview` inspections, cleared before the final report (#170)
- Added width-aware human reports for interactive terminals and deterministic 80-column wrapping for redirected output (#166)
- Added TTY-safe semantic color and compact interactive branding with hardened capability gating and documented `NO_COLOR`, keeping redirected output and JSON plain (#164)
- Added focused-help aliases, canonical Host guidance, and bounded command suggestions for unknown commands; fixed unknown-command self-suggestions, nested root-help aliases, and focused-help alias parsing (#158)
- Added read-only `list projects` views in human and versioned JSON forms, staying available when individual configured roots are invalid (#160)
- Added read-only `list profiles` views with deterministic Profile IDs and artifact counts in human and versioned JSON forms (#161)
- Added read-only `list temporary` views for Temporary Profile Installation identity recovery (#162)
- Added read-only `list hosts` views with canonical Host order and temporary-installation eligibility (#163)
- Added read-only `info` views for the engine version and Workspace, Local Configuration, and Installation State locations, reporting legacy Local Configuration explicitly (#157)
- Changed root help to responsive workflow groups with width-aware two-line command entries (#156)
- Fixed Temporary Profile Installation blocked errors to derive from one canonical normalized collection, and migrated remaining Installation State, Git exclusion, and reconciliation blockers to complete structured evidence (#169)
- Fixed command summaries so uninstall describes removing proven Agent Profile Kit-owned output and no-Project status points to inventory and binding (#165)
- Changed tracked-output ownership conflicts to one typed Project-scoped blocker with a capped affected-path list and an overflow pointer to `--verbose` (#168)
- Changed Adapter Host capability and preflight blockers to structured evidence while keeping unexpected preflight failures distinct (#167)
- Changed bare `apkit guide` to print a concise topic index, with the complete guide at `apkit guide --full` (#159)
- Changed the shared blocker contract to normalized structured evidence, hardening fallback, validation diagnostics, and blocked-report projection (#155)
- Added Claude Code parity for `install-temp`/`remove-temp`, reusing the Claude Adapter plan and the versioned receipt protocol (#136)
- Added durable recovery for Temporary Profile Installations: recovery identity before owned mutations, `removalRequired` failure reporting, disposable removal of modified owned roots, and a lifecycle lock shared with `apply`/`uninstall` (#137)
- Added `install-temp` and `remove-temp` for temporary Codex Profile installation into one explicit Project with a versioned JSON receipt and idempotent removal (Installation State schema v5 — back up `~/.agents/agent-profile-kit/state/manifest.yaml` before the first 0.50.0+ write, restore it before downgrading) (#135)
- Added `--json` machine output for `preview`, `apply`, and `status`, with uniform exit codes: 0 clean, 1 tool error, 2 blockers; `status` no longer exits 0 while reporting blockers (#126)
- Changed Context-bearing Profile Installations to require Codex CLI 0.145.0+, emitting SessionStart hooks with `additionalContextLimit: 0`; older installs fail preflight before any write — upgrade Codex and re-apply, or temporarily remove the binding (#138)
- Fixed project-scoped blocker text to render bound Project references through one shortest-unambiguous path presenter across `bind`, lifecycle, warnings, and diagnostics (#152)
- Fixed resumed Codex conversations duplicating Profile Context by excluding `resume` from the SessionStart hook matcher; injection remains on `startup`, `clear`, and `compact` (#139)
- Fixed informational Host Setup Steps to require no user action, and locked Grok's compatibility-disabled path at the Adapter seam (#145)
- Improved mixed-fleet intended teardown status wording, canonicalized teardown Hosts, and kept uninstall Git-exclusion receipts concise (#144)
- Fixed combined content-and-mode member drift deduplication at reconciliation and made verbose Context fences collision-safe (#143)
- Improved mixed-project guidance to reflect the all-project apply gate and deduplicate verbose state explanations (#142)
- Fixed `init` next steps when adopting an existing Workspace, made example binding guidance project-scoped, and diagnosed partial example removal (#141)
- Added conditional Grok shared-rule and Pi project-trust Host Setup Steps, with explicit confirmation when a changed installation requires no further setup (#125)
- Added deliberate-teardown reporting: `uninstall` lists affected Projects, removed paths, cleaned exclusions, and preserved bindings, and `status` reports intended teardown (Installation State schema 4 provenance) (#124)
- Added a scaffolded example Profile and Context Module on first `init` with short `guide` topics for each artifact type (#121)
- Added reconciliation-plan path markers in concise lifecycle reports with a `--verbose` overflow cap (#120)
- Improved missing-Profile errors to name available Profiles, empty Workspaces to point at authoring, and `validate` to list Profiles and Hosts (#119)
- Added typed Host Setup Steps from Adapters through lifecycle reports, replacing the prior `validate` warning (#118)
- Changed blocked lifecycle reports to lead with one actionable blocker and documented the generated-file drift recovery loop (#117)
- Fixed edited generated output preservation after a Marker-proven project move (#117)
- Improved project lifecycle blocks to identify Projects with short unambiguous paths and each binding's Hosts (#116)
- Added `--version`, conventional root-help aliases, complete per-command help, and fail-fast leading-dash argument handling (#115)
- Added the CLI user journey document with a stable-ID gap register (`docs/USER-JOURNEY.md`, ADR-0013)
- Added ADR-0014 recording the user-facing CLI presentation decisions — vocabulary layering, information design, Host Setup Steps, uniform exit codes
- Added the UI lexicon for task-focused default lifecycle wording, keeping internal domain vocabulary in `--verbose` output (#114)
- Added Pi-native Skill projection preserving disabled model-invocation policy with explicit Artifact ID activation and invocation-specific Capability Contracts (#104)
- Changed Pi invocation-capable `host_versions` reads to require Agent Profile Kit 0.34.0+ — unbind Pi or re-apply before rolling back (#104)
- Changed Pi Skill capability preflight to be settings-aware, failing closed on configured Skill paths, dynamic extensions, and unprovable configuration (#103)
- Added Pi Skill installation under `.pi/skills/<Artifact ID>/` with static discovery collision proof beneath managed Skill trees (#102)
- Changed Pi Skill-capable `host_versions` reads in Installation State to require Agent Profile Kit 0.32.0+ (#102)
- Added Pi as a project-bound Agent Host for Context-only installations through `.pi/APPEND_SYSTEM.md` with 0.82.1+ preflight (#101)
- Changed Installation State containing the `pi` Host to require Agent Profile Kit 0.31.0+ (#101)
- Changed repeated Host entries to normalize at Local Configuration and `bind` ingestion to the canonical supported-Host order (#101)
- Added next-lifecycle-action guidance on concise `status` and `preview` results, with one conservative aggregate instruction for multi-Project outcomes (#90)
- Added concise lifecycle summary terminology explanations with `--verbose` exposing complete diagnostics (#89)
- Added Grok Skill installation under `.grok/skills/<Artifact ID>/` with fail-closed discovery preflight across native, personal, compatibility, and plugin sources (#87)
- Added Grok as a project-bound Agent Host for Profile Context via always-scanned `.grok/rules/` (#86)
- Added command explanations to root `apkit` and `apkit --help` with an `init` → `bind` → `preview` → `apply` quick start, and error-naming for unknown commands (#88)
- Added repair of wholly absent owned files and artifact directories from Workspace source when the Installation Marker and surviving outputs prove ownership (#79)
- Added Repository Exclusion Records as the canonical machine-local ownership source for shared Git exclusion files, with a one-time schema-v2 state migration publishing schema v3 (#77)
- Added concise outcome-led `bind`, `preview`, `apply`, and `status` output with `--verbose` lifecycle diagnostics (#70)
- Added an optional explicit Workspace path to `apkit init`, adopting valid existing Workspaces and failing closed on conflicts (#69)
- Changed Local Configuration schema version 2 to require an explicit Workspace selection, migrating supported version-1 configuration (#68)
- Changed Workspace selection to reject paths overlapping Local Configuration or disposable installation state, including symlinked paths (#73)
- Added the 0.24.2 Installation State backup and pre-delete `apply` documentation required for rollback and retirement of pre-0.24.2 non-Git installations (#78)
- Added intended-teardown provenance to Installation State schema 4 with schemas 2 and 3 as non-mutating migration inputs (#124)
- Changed member-level attention statuses to authoritative in verbose output (#123)
- Changed apply output headings to `Pending:`/`Applied:`, removed the duplicate current-status line, and named per-Project next actions (#122)
- Changed default and verbose Git exclusion repair receipts to derive from one canonical line shape (#128)
- Changed the published CLI command to `apkit`; the former `agent-profile-kit` executable is no longer installed (#113)
- Changed Installation Manifest `engine_version` to provenance rather than desired state, so a version bump alone no longer re-applies a current installation (#113)
- Fixed apply receipt reporting for verbose repairs, per-Project output, and post-commit verification failures, reporting the verified post-apply state separately from the reconciliation receipt (#97)
- Fixed omitted Codex lifecycle hook settings to default-enable, honor `CODEX_HOME`, and fail closed on malformed settings (#84)
- Improved compact lifecycle summaries to report safely repairable output distinctly (#83)
- Fixed retirement of intentionally deleted projects after exact-path `unbind` without requiring a vanished Installation Marker, preserving shared Git exclusion ownership (#78)
- Fixed Installation State restore failures surfacing during transactional `apply` and `uninstall` instead of being silently discarded (#77)
- Fixed reconciliation to bind each Project only at its exact canonical root without enrolling sibling Git worktrees (#76)
- Added the required pre-migration Local Configuration backup and restore documentation for schema-v2 downgrade (#68)
- Fixed Host Setup Step paths rendering through the canonical path presenter and suppressed activation claims after no-op applies (#132)
- Fixed Skill discovery, precedence, deduplication, and collision handling to delegate to Codex, Claude, Grok, and Pi Host Resolution, reporting detectable disabling or malformed Host configuration as warnings (#109)

## [0.20.0] - 2026-07-16

- Added recording-only `bind <profile> [project] --host <host>…` appending one validated Project Binding to Local Configuration without reconciling Project or Host state (#54)
- Added recording-only `unbind [project]` removing one Project Binding by canonical identity, leaving generated output for `preview`/`apply` (#56)
- Added an optional Local Configuration `workspace` path selecting one existing Workspace, defaulting to `~/.agents/agent-profile-kit/workspace/` (#51)
- Added permanent packed-CLI release-candidate gates and neutral qualification fixtures covering model-invocation policy, Skills-only Profiles, Skill identity collisions, and optional Workspace scaffolding (#35)
- Added Skills-only Profiles: a Profile needs at least one supported artifact overall, and Skills-only bindings skip Context outputs and Context capability requirements (#55)
- Changed universal artifacts to remain canonical Workspace source with Profile selection driving only project-bound delivery; selected↔global Skill collisions stay blocked and dual delivery fails closed (#52)
- Changed Workspace scaffolding to be optional after initialization: only a supported `workspace.yaml` is required, and missing artifact directories ingest as empty categories (#50)
- Fixed dangling Workspace category symlinks to be rejected as structural errors instead of empty categories (#50)
- Added the final project-bound public overview, Workspace guide, and agent workflow (#34)
- Fixed selected project-bound Skills colliding with a Host's personal/global Skill identity to fail closed, with `status` reporting later overlaps as blocked (#53)
- Changed packaging to keep personal Workspace and generated migration content out of the public package with packed-artifact boundary checks (#16)
- Added the Claude Capability Contract `native-project-unscoped-rules-skills-v1` for installations proving unscoped project rules and native Skill discovery (#42)
- Added optional Skill model-invocation policy via `metadata.agent-profile-kit.model-invocation` with Adapter-owned projection and capability preflight (#49)
- Added Claude Skill installation at project scope under native `.claude/skills/<Artifact ID>/` discovery with transitive dependency selection and inclusion reasons (#42)
- Added Profile Context installation for Claude Code as an owned `.claude/rules/agent-profile-kit.md` rule (#32)
- Added Codex Skill installation at project scope under native `.agents/skills/<Artifact ID>/` discovery with transitive dependency selection (#31)
- Changed Adapter plans and reconciliation to own complete artifact directories as one ownership boundary (#39)
- Added Git worktree expansion and repository-local generated-path exclusions (#30)
- Added deterministic global reconciliation with normalized multi-Adapter output plans and independently transactional Project updates (#29)
- Added the project-bound Context-only Codex lifecycle with Local Configuration ingestion, preview/apply reconciliation, and safe uninstall (#28)
- Added typed transitive cross-artifact dependency resolution with auditable Manifest inclusion reasons (#7)
- Added the schema-versioned `init` CLI and npm executable contract (#2)
- Added bundled human and agent Workspace authoring guides (#3)
- Added Context-only Codex Profile validation, planning, installation, and launch (#4)
- Added transactional Profile Installation status, update, and verified uninstall lifecycle management (#5)
- Added standard Skill package ingestion and validation groundwork (#6)
- Added the owned Codex Skill Library with transactional complete-Workspace projection and conflict protection (#6)
- Changed Profiles to define observable artifact selection while Adapters deliver natively per artifact category (#6)
- Removed the unreleased per-session launcher and global Skill projection in favor of native project SessionStart output (#28)
- Changed Workspace staging to clean only output owned by the running initialization process, and initialization warnings to their single concurrent-cleanup producer (#18)
- Fixed init to report incomplete Workspace structure, malformed YAML, unsupported schema versions, and dangling symlinks with actionable errors without modifying user-owned source (#18)
- Fixed init staging cleanup to stay non-blocking, preserve initialization failures, expose nested cleanup failures, and recognize valid symlinked Workspaces (#18)
- Fixed concurrent initialization to converge, atomically replace empty Workspace directories, and report empty symlink targets clearly (#18)
- Fixed init to quote optional shell commands safely and enforce initial macOS support at package installation (#18)
- Fixed missing repository-local exclusion ownership reporting before repair, publishing exclusion changes only after Manifest durability (#38)
- Improved repairable-marker status and malformed-state apply error precision (#37)
- Fixed recovery from interrupted Profile Installation replacement without blocking later updates (#21)
- Fixed `status` reporting an existing Profile Installation without modifying it (#20)
- Removed the unreleased `plan`, `install`, `update`, and `run` interfaces, launcher, leases, and global Codex Skill projection (#28)
